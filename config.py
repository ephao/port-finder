# Default host to check for unused ports
DEFAULT_HOST = 'localhost'

# Minimum and maximum allowed port numbers
MIN_PORT = 1
MAX_PORT = 65535

# Default port range to scan if not specified
DEFAULT_START_PORT = 8000
DEFAULT_END_PORT = 9000
